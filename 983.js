/**
 * @param {number[]} days
 * @param {number[]} costs
 * @return {number}
 */
 var mincostTickets = function(days, costs) {
    let weekCost = 0, monthCost = 0, minCost = 0, cost = 0
    for (const day of days) {
        
    }
};